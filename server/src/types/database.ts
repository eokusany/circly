// Run `npm run gen:types` to regenerate this file from your Supabase schema.
// Requires: SUPABASE_PROJECT_ID env var and supabase CLI authenticated.
//
// Usage:
//   import type { Database } from './types/database'
//   const supabase = createClient<Database>(url, key)

export type Database = Record<string, never>
